name: CI - Build Scan & Push Finote Frontend Image

on:
  push:
    branches:
      - main
      - dev

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io

jobs:
  build-scan-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build app
        run: npm run build

      # ------------------------
      # Docker Preparation
      # ------------------------
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Read VERSION
        run: echo "VERSION=$(cat VERSION)" >> $GITHUB_ENV

      - name: Set short SHA
        run: echo "SHA=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Set image name (lowercase)
        run: |
          REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_NAME=ghcr.io/${REPO_LOWER}/finote-frontend" >> $GITHUB_ENV

      - name: Set image tags
        run: |
          SHORT_SHA=${GITHUB_SHA::7}

          if [[ "${GITHUB_REF_NAME}" == "main" ]]; then
            echo "IMAGE_TAGS<<EOF" >> $GITHUB_ENV
            echo "${IMAGE_NAME}:latest" >> $GITHUB_ENV
            echo "${IMAGE_NAME}:${VERSION}" >> $GITHUB_ENV
            echo "${IMAGE_NAME}:${VERSION}-${SHORT_SHA}" >> $GITHUB_ENV
            echo "EOF" >> $GITHUB_ENV
          else
            echo "IMAGE_TAGS<<EOF" >> $GITHUB_ENV
            echo "${IMAGE_NAME}:dev" >> $GITHUB_ENV
            echo "${IMAGE_NAME}:${VERSION}-dev-${SHORT_SHA}" >> $GITHUB_ENV
            echo "EOF" >> $GITHUB_ENV
          fi

      # ------------------------
      # BUILD (no push)
      # ------------------------
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          load: true
          tags: ${{ env.IMAGE_NAME }}:ci
          no-cache: true

      - name: Debug node_modules inside image
        run: |
          docker run --rm ${{ env.IMAGE_NAME }}:ci \
            npm ls glob cross-spawn tar || true

      # ------------------------
      # SECURITY SCAN
      # ------------------------
      - name: Trivy Scan
        uses: aquasecurity/trivy-action@0.20.0
        with:
          image-ref: ${{ env.IMAGE_NAME }}:ci
          severity: HIGH,CRITICAL
          exit-code: 1

      # ------------------------
      # LOGIN
      # ------------------------
      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      # ------------------------
      # SBOM Software Bill of Materials
      # ------------------------
      - name: Generate SBOM (CycloneDX)
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.IMAGE_NAME }}:ci
          format: cyclonedx-json
          output-file: sbom.json

      # ------------------------
      # PUSH
      # ------------------------
      - name: Push Docker image
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: ${{ env.IMAGE_TAGS }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
